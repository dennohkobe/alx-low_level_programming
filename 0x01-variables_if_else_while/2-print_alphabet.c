/*
 * Printing alphabet
 */

#include <stdio.h>

/**
 * main function description
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	putchar(letter);
	putchar('\n');
	/* return desc */
	return (0);
}
